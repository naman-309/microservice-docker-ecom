main.jsx = React application ko start karne wali file

main.jsx
   ↓
React ko bolo:
"App ko root ke andar render karo"

App.jsx

Ye hamara main React component hai.


vite.config.js is the configuration file used to customize and configure how Vite builds and runs a frontend application.


Component kya hai?

A component is a reusable piece of UI in a React application.


Frontend mein:

URL
 ↓
React Router
 ↓
Page Component
 ↓
UI


<div id="root"></div>

React isi empty div ke andar poori application inject karta hai.

Definition: Link is a React Router component used for navigation between routes without a full browser page reload.

Pages      = poora page
Components = page ke chhote parts
Common     = jo parts baar-baar use hon